# witkit 妙计科技官网 · 设计原则（第 1 层）

> 采集：2026-09-19 ｜ 修订：2026-09-19（视觉方向推翻重做，见「修订记录」）
> 状态：已确认

## 基础信息

| 项 | 内容 |
|---|---|
| 项目 | 妙计科技（witkit）官方网站 |
| 领域 | 科技 / 企业服务（B 端门面 + 开发者生态） |
| 目标用户 | 潜在客户、合作方、开发者 |
| 第一印象 | 大厂、专业、有技术实力 |
| 最重要动作 | 了解三大技术体系与核心产品 |
| 定位 | 真实业务官网，要上线运营，对标大厂质量 |

## 设计原则（按优先级）

### 1. 大厂质感 = 克制，不是堆料

留白、层级、细节。**"土"的三大来源**——饱和度失控、装饰过多、动效抢戏——全部禁止。

### 2. 白底为主，蓝色只出现在该被看到的地方

全站单一品牌蓝（`#0052D9`），只用于：主按钮、链接、关键数字、区块小标签。**不铺满、不做渐变背景、不做发光。**

### 3. 深色是区块，不是主题

不做明暗切换。深色（`#0B0F19`）只出现在 CTA 带和页脚，用来做视觉收口。

### 4. 真实数据撑可信度

99.99% 可用性、<10ms 延迟这些指标直接上首页，不编数字。

### 5. 装饰语言 = 工程图纸

装饰必须像**技术图纸**，不能像游戏启动页：
- ✅ 蓝图网格、十字准星、断线角框、刻度尺、标注引线、竖排文字、线条逐笔绘制
- ❌ 鼠标跟随光晕、粒子飘散、标题流光、霓虹发光、扫描线、故障失真、棱镜色散

判断标准一句话：**这个元素出现在工程图纸上合理吗？**

## 色彩

| 用途 | 值 | 说明 |
|---|---|---|
| 页面底 | `#FFFFFF` | 纯白 |
| 交替区块底 | `#F6F7F9` | 浅灰，区分节奏 |
| 卡片底 | `#FFFFFF` | 配 1px 边框 |
| 深色区块 | `#0B0F19` | CTA 带、页脚 |
| 主文字 | `#0B0F19` | 近黑，不用纯黑 |
| 次要文字 | `#4E5969` | 描述、正文 |
| 弱化文字 | `#6B7280` | 辅助说明（对比度 4.83:1 ✓） |
| 品牌蓝 | `#0052D9` | 主按钮、链接、关键数字（对比度 6.55:1 ✓） |
| 蓝 hover | `#0043B5` | |
| 装饰亮蓝 | `#006EFF` | **仅用于图形**，不承载小字（对比度不足 4.5） |
| 浅蓝底 | `#EEF4FF` | 标签、序号徽标 |
| 分隔线 | `#E5E8EF` | |

对比度全部通过 `src/styles/check.mjs` 自动校验，改色必跑。

## 字体

**系统字体栈，不加载外部 Web Font。**

```
'PingFang SC', 'HarmonyOS Sans SC', 'Microsoft YaHei',
-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif
```

理由：Google Fonts 在国内不稳定，拖慢首屏；中文系统字体渲染质量已足够。数字统一 `tabular-nums`。

字号刻度：12 / 14 / 16 / 18 / 20 / 24 / 32 / 40 / 56 / 72。

## 动效（只允许三种）

| 动效 | 参数 | 用途 |
|---|---|---|
| 滚动渐入 | `opacity 0→1` + `translateY 12px→0`，520ms | 所有区块 |
| 数字滚动 | 1.5s，三次缓出 | 数据指标 |
| 线条逐笔绘制 | 900ms，SVG `pathLength` 归一化 | 架构图 |

**没有别的动效。** 所有动效遵守 `prefers-reduced-motion`。

## 站点地图

```
/                    首页
/tech/hec            技术体系 HEC ┐
/tech/uef            技术体系 UEF ├─ 三个都必须完整，不能只做一个
/tech/cep            技术体系 CEP ┘
/product/:slug       产品详情（13 个产品共用）
/about               关于我们（含 #contact 联系表单）
```

## 技术方案

- **前端**：Vue 3 + Vite + TS + Tailwind CSS v4 + vue-router
- **后端**：FastAPI + PostgreSQL，`witkit-api/contracts/openapi.yaml` 契约先行
- **Mock**：MSW，前端不等后端
- **Token 单一源**：`src/styles/tokens.json` → `build.mjs` → `tokens.css`

## 硬性约定

1. 改任何色值/尺寸 → 改 `src/styles/tokens.json`，跑 `node src/styles/build.mjs`，**不手改** `tokens.css`
2. 改完必跑 `node src/styles/check.mjs`，对比度 + 裸色值必须全绿
3. 列表接口返回完整对象（官网数据量小，首页需要完整字段）——见 openapi.yaml 注释
4. 图片未就位时，用工程图纸风格占位图形兜底，不允许出现空洞或裂图

## 修订记录

- **2026-09-19 初版**：白底 + 品牌蓝 + 明暗双主题
- **2026-09-19 修订**：用户反馈"廉价、土、鼠标光晕像电灯"。**推翻**：
  - 删除明暗双主题 → 改为白底单主题，深色仅作区块
  - 删除鼠标跟随光晕、粒子漂浮、标题流光
  - 删除外部 Web Font（Inter / Sora / JetBrains Mono）
  - 确立"工程图纸"装饰语言
  - 三大技术体系从"三个小卡片"改为"三个完整区块"
  - 参照物：腾讯官网

## 附：产品与技术体系清单

### 三大技术体系

| 体系 | 全称 | 定位 |
|---|---|---|
| HEC | Hyper Evolution Connect | 全域互联与统一授权底座 |
| UEF | Ultra Evolution Framework | AI 驱动的自研全栈执行架构 |
| CEP | Cloud Evolution Platform | 自研企业级云能力平台 |

### 核心产品

E时代IDE、E时代云服务、WeAuth 微验、云数据库（ECD）、云存储（EOSS）、信任中心

### 生态产品

云剪贴板、比赛报名系统、图床、论坛、Git

### 实验室

两个都在站外，官网只做外链，不给它们配图。

| 名字 | 是什么 | 地址 |
|---|---|---|
| 妙计实验室 | 母体，做前沿技术开发与算法研究；它自己的站点写着旗下有启发实验室与 E时代科技 | https://home.miaojilab.cn |
| 启发实验室 | E时代协会的算法与开发中心（社团性质），IDE 出自这里 | https://www.qifalab.cn/qifalab-v1/ |
