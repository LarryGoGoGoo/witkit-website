# witkit 妙计科技官网 · 图片需求清单

> 这份文件由 `src/assets/imageSpecs.json` 自动生成，不要手改。改规格请改那个 json，然后跑 `npm run docs:images`。

做好的图丢进 `src/assets/images/` 对应位置，**文件名对上就自动生效**，不用改代码、不用改配置，刷新即出。

**没做的图不会报错、不会裂图** —— 页面用工程图纸风格的占位框兜住，框里直接写着这张图要什么。可以分批补。当前共 18 个站内图片位 + 2 个 public 文件。

另有 14 位规格保留、**当前页面没有任何地方引用**（用到的板块已经删除），不计入上面这个数：`hero-bg`、`hero-word`、`icons/ide`、`icons/cloud`、`icons/weauth`、`icons/ecd`、`icons/eoss`、`icons/trust`、`labs/miaoji`、`labs/qifa`、`careers/office`、`careers/culture-1`、`careers/culture-2`、`careers/culture-3`。以后要重做产品矩阵 / 实验室那类版式，可以直接拿来用。

随时查进度：`npm run check:images`

---

## 必做 · 影响第一眼观感

| 文件名 | 放哪 | 尺寸 | 比例 | 内容要求 |
|---|---|---|---|---|
| `logo-witkit` | 全站顶栏 | 630 × 120 px（由用户原图裁边缩放而来，源图 1923 × 817） | 5.25:1 | witkit 妙计 横版双语 lockup · 蓝字版（浅底用）。字标就是「witkit」+「妙计」两个词，不带「科技」二字 —— 这是定稿口径，不是漏字，别去补 |
| `logo-witkit-inverse` | 全站页脚 | 630 × 120 px（由 logo-witkit 自动生成的反白版） | 5.25:1 | 同上，白字透明底版（深色底用）。字标同样只有 witkit + 妙计。用户提供正式反白版后直接替换即可 |
| `products/ide` | 产品页卡片 · IDE 详情页 | 1440 × 900 px | 16:10 | 编辑器主界面：代码区 + AI 侧栏 + 文件目录树 |
| `products/cloud` | 产品页卡片 · 云服务详情页 | 1440 × 900 px | 16:10 | 云控制台总览：资源卡片 + 用量图表 |
| `products/weauth` | 产品页卡片 · WeAuth 详情页 | 1440 × 900 px | 16:10 | 验证组件嵌入业务页面的实际效果，或验证管理后台 |
| `products/ecd` | 产品页卡片 · 云数据库详情页 | 1440 × 900 px | 16:10 | 图标 + 名称 + 副标题的产品识别图：图标=数据库，名称 E时代云数据库，副标题 Enterprise Cloud Database（取自 ECD 控制台真实标题） |
| `products/eoss` | 产品页卡片 · 云存储详情页 | 1440 × 900 px | 16:10 | 图标 + 名称 + 副标题的产品识别图：图标=云朵，名称 E时代云存储，副标题 企业级对象存储服务（取自 EOSS 控制台真实标语） |
| `about/company` | 关于页 · 公司简介整屏页配图（左字右图） | 1600 × 1000 px | 8:5 | 办公楼外观或办公环境实拍。使用实拍照片，不用素材图库中的通用办公楼。横构图 |
| `about/vision` | 关于页 · 愿景及使命整屏页配图（左字右图） | 1600 × 1000 px | 8:5 | 团队工作场景：白板讨论、结对编程、屏幕前的侧影。画面里要有人正在做事，不使用合影摆拍 |

## 建议 · 页面更「有东西」

| 文件名 | 放哪 | 尺寸 | 比例 | 内容要求 |
|---|---|---|---|---|
| `services/overview` | 服务页（/services）头部主视觉 | 1600 × 900 px | 16:9 | 三层技术体系总览图：HEC / UEF / CEP 之间的关系与分层 |
| `systems/hec` | 服务页 /hec 体系页的图侧（与 TechDiagram 画的架构图并列） | 1600 × 1000 px | 8:5 | 体系示意图。当前放的是矢量示意图（架构图候选/services-*.svg 分化而来）；有成品渲染图可直接替换，文件名对上即生效 |
| `systems/uef` | 服务页 /uef 体系页的图侧（与 TechDiagram 画的架构图并列） | 1600 × 1000 px | 8:5 | 体系示意图。当前放的是矢量示意图（架构图候选/services-*.svg 分化而来）；有成品渲染图可直接替换，文件名对上即生效 |
| `systems/cep` | 服务页 /cep 体系页的图侧（与 TechDiagram 画的架构图并列） | 1600 × 1000 px | 8:5 | 体系示意图。当前放的是矢量示意图（架构图候选/services-*.svg 分化而来）；有成品渲染图可直接替换，文件名对上即生效 |
| `about/value-open` | 关于页 · 企业文化整屏页背景（第 1 条 开放，字压在图上面） | 2880 × 1620 px | auto | 与「开放」呼应：公开的资料墙、多人围桌讨论、白板上的分歧。整屏铺满用，画面要深色调、上半部留得出白字空间 |
| `about/value-inclusive` | 关于页 · 企业文化整屏页背景（第 2 条 包容，字压在图上面） | 2880 × 1620 px | auto | 与「包容」呼应：各不相同的工作台、不同风格的人在协作。整屏铺满用，深色调、留得出白字空间 |
| `about/value-sincere` | 关于页 · 企业文化整屏页背景（第 3 条 真诚，字压在图上面） | 2880 × 1620 px | auto | 与「真诚」呼应：手写便签、被划掉重写的方案稿、真实的工作台细节。整屏铺满用，深色调、留得出白字空间 |
| `about/value-passion` | 关于页 · 企业文化整屏页背景（第 4 条 热爱，字压在图上面） | 2880 × 1620 px | auto | 与「热爱」呼应：专注的侧影、屏幕前的深夜、自己产品正在被使用的画面。整屏铺满用，深色调、留得出白字空间 |

## 可选 · 上线前补或不补都行

| 文件名 | 放哪 | 尺寸 | 比例 | 内容要求 |
|---|---|---|---|---|
| `products/trust` | 产品页 · 信任中心详情页 | 1440 × 900 px | 16:10 | 图标 + 名称 + 副标题的产品识别图：图标=盾牌，名称 E时代信任中心，副标题 TRUST CENTER（取自信任中心站真实标题） |

## public/ 下的文件（不走 src/assets）

| 文件 | 放哪 | 尺寸 | 内容要求 |
|---|---|---|---|
| `/favicon.png` | 浏览器标签页图标 | 512 × 512 px | witkit 图形标识（W 字母或品牌符号），必须正方形 |
| `/og-cover.png` | 微信 / 群聊分享缩略图 | 1200 × 630 px | 左侧留 logo 位，右侧一句主张（如「三大自研技术体系」） |

> 这两个文件放在 `public/` 根目录。favicon 换好后，把 `index.html` 里 `apple-touch-icon` 那行的注释打开；og 封面同理。

---

## 通用规范

| 项 | 要求 |
|---|---|
| 命名 | **全小写 + 连字符**，不用中文、不用空格、不加 `@2x` 后缀 |
| 扩展名 | `png` / `jpg` / `jpeg` / `webp` / `avif` / `svg` 都行，代码按同名匹配，不看扩展名 |
| 分辨率 | 一律按 **2x** 出图（显示 1440 宽的，出 2880），高清屏不糊 |
| 体积 | 单张 **< 300KB**。PNG 用 tinypng 压，照片转 WebP |
| 截图 | 用**真实界面**，浅色主题，和官网白底一致；真实邮箱、密钥、Token 必须打码 |
| 版权 | 必须是自有素材或已授权，别用网上随手存的图 |

## 为什么不用 public/images/

1. **没做的图不会产生 404 请求**。构建期就确定哪些图存在，不存在的直接渲染占位框，控制台一个报错都没有。
2. Vite 会给每张图加内容指纹，浏览器缓存可以永久有效，改图后用户必然拿到新版。

## 当前页面上能看到这些占位框的位置

> 这张表按页面实际引用的图片位写，改版式时如果动了图片位，记得一起改（脚本里就是几行字符串）。

| 页面 | 图片位 |
|---|---|
| 首页 `/`（＝关于我们） | 首屏纯黑底无配图（2026-09-20 起不用图片）、公司简介配图 ×1、愿景配图 ×1、企业文化背景 ×4（一屏只显示当前那条） |
| 我们的服务 `/services` | 体系总览图 ×1、三大体系图 ×3 |
| 我们的产品 `/products` | 核心产品卡 ×7、组合说明图 ×1（复用服务页总览图） |
| 新闻动态 `/news` | 每条动态一张封面（还没有稿件，所以现在页面里 0 个占位框） |
| 加入我们 | 已并入「联系我们」锚点（/careers 页已删除，办公环境图与三联图不再使用） |
